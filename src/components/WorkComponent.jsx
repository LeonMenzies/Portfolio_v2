const StyledSection = styled.div`
  padding: 1rem;
`;

const WorkComponent = ({
  title = "title",
  location = "location",
  details = [],
}) => {
  return <StyledSection>

    
  </StyledSection>;
};
export default WorkComponent;
